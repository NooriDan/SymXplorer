from setuptools import setup, find_packages

setup(
    name = "MacAnalog-Symbolix",
    description = "A python app to perform symbolic analysis on analog circuits", 
    version = "0.1",
    packages = find_packages(where="MacAnalog-Symbolix")
)